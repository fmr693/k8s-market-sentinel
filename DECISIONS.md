# Decisiones de arquitectura — k8s-market-sentinel

> Registro de las decisiones que resuelven las **"Dudas abiertas"** de [PROJECT_BRIEF.md](PROJECT_BRIEF.md).
> Fecha: 2026-07-06. El brief sigue siendo el contexto completo; este documento fija las respuestas y el estado.

## Estado general

- ✅ **Fase de decisiones cerrada** — 7/7 dudas resueltas (la #7 marcada como provisional).
- ✅ **Fase 1 completada** (2026-07-06): esquema medallón en **Neon** + 4 ingestores validados en la nube con idempotencia comprobada — precios yfinance (60.780 velas), FRED (3.707 obs.), FX BCE (2.946 fixings) y **NAV CEFConnect** (4.646 NAVs diarios, 19/19 CEFs). La capa gold produce **descuentos y z-scores reales** que coinciden al céntimo con los publicados por CEFConnect (validación externa por camino independiente).
- ✅ **Fase 2 completada** (2026-07-06): imagen única `sentinel:dev` (python:3.13-slim, non-root, 481 MB) con las 5 caras del CLI; validada ejecutando `migrate` + ingestas reales contra Neon desde el contenedor.
- ✅ **Fase 3 completada en k3d local** (2026-07-07): namespace + Secret/ConfigMap generados por kustomize + 4 CronJobs con `timeZone: Europe/Madrid`; validado end-to-end (Job de migración + CronJob disparado a mano ingiriendo contra Neon desde el clúster). **Pendiente**: replicar en el k3s del Ubuntu (requiere publicar la imagen en GHCR).
- ✅ **Imagen publicada en GHCR** (2026-07-07): `ghcr.io/fmr693/k8s-market-sentinel:0.1.0` (+`latest`, digest `76cc7768…`), paquete **público**. Verificado con pull anónimo (logout + rmi + pull limpio: baja sin credenciales). Manifests apuntados a GHCR (transformador `images:` en kustomization.yaml; `job-migrate.yaml` a mano por vivir fuera) y runbook completo en `deploy/DEPLOY_UBUNTU.md`. **La Fase 3 queda a un solo paso: ejecutar el runbook en el servidor Ubuntu cuando haya acceso (hoy está virgen e inaccesible).**
- ✅ **Fase 4 VALIDADA** (2026-07-07 tarde, mercado abierto): poller en local ~4,5h (gap-fill 82.724 velas 1m + 49 ticks, sobrevivió a una suspensión del portátil y retomó solo) y en k3d con la 0.3.0 (gap-fill 90.778 + ticks de ~10k velas; **SIGTERM limpio verificado** con `kubectl delete pod`: "SIGTERM recibido, salida limpia" y sustituto Running en 4s). Bonus: el CronJob de FX de las 16:45 **se disparó solo** en k3d y recogió el fixing del día.
- ✅ **Fase 5 VALIDADA CONTRA NEON** (2026-07-07 tarde): rol `grafana_ro` creado en Neon (verificado: lee gold, el INSERT falla con InsufficientPrivilege); migración 0005 aplicada; backfill de las 20 stocks (44/44 tickers, ~58k velas nuevas); Grafana verificado por API **en local (compose) y en k3d**: health OK, 2 dashboards aprovisionados, datasource conectado a Neon, queries reales de panel (snapshot: 19 CEFs, 14 en descuento) e intradía en vivo (37-43 tickers en los últimos minutos).
- ✅ **Mini-fase YIELD completada** (2026-07-07/08): migración 0006 (`silver.distributions` + vista `gold.yield_ttm`), 4º ingestor con el patrón de la casa (19/19 CEFs, 2.113 distribuciones desde 2015), CronJob `ingest-distributions` (23:15), panel "Yield & distributions (TTM)" en CEF Sentinel e imagen **0.4.0** corriendo en k3d. Sanity contra Neon: yields plausibles (crédito ~10-16% sobre precio), FSCO 15,8% precio vs 10,9% NAV (la brecha ES el descuento) y la señal de recorte detecta 4 casos reales (BST el más severo: ritmo actual -29% vs TTM). Verificado end-to-end por la API de Grafana con el rol `grafana_ro` (los default privileges de la decisión #28 cubrieron solos los objetos nuevos).
- ✅ **Imagen 0.4.0 publicada en GHCR** (2026-07-08): `ghcr.io/fmr693/k8s-market-sentinel:0.4.0` + `latest`, digest `2b6d664f…` idéntico al del build local validado en k3d. Lección de infra del desbloqueo: el PAT era correcto desde el principio — el `docker login` fallaba porque el **pipe de Windows PowerShell 5.1 corrompe la contraseña** (codificación OEM + CRLF final); con `printf '%s' TOKEN | docker login --password-stdin` desde Git Bash (bytes exactos) entró a la primera. Cuando una credencial "correcta" falla, sospechar del transporte antes que del secreto.
- 🔄 **REENCUADRE ESTRATÉGICO (2026-07-08):** el proyecto no compite en producto financiero (CEFConnect/CEFData ya publican descuentos y z-scores); compite en **arquitectura portable**. La tesis financiera es la carga útil demostrativa; lo que se deja "a prueba de bombas" es el patrón: medallón, ingesta idempotente autorreparable, config-driven, crash-only, dashboards como código, y (nuevo orden) CI/CD y GitOps ANTES que más features de dominio. Roadmap redibujado abajo: las fases 6-7 originales se reordenan y amplían a 6-11.
- ✅ **Fase 6 (CI/CD) COMPLETA** (implementada 2026-07-08, publicada 2026-07-10): `uv.lock` (42 paquetes con hashes, uv 0.9.30), Dockerfile por capas (deps → código, editable, `USER 1000`) y workflow de dos jobs (`test` en cada push/PR; `build-push` solo en tags `v*`, con guardia tag==pyproject y smoke antes del push, auth `GITHUB_TOKEN`). El bloqueo de publicación era un permiso manual: el paquete lo creó el PAT en la saga 0.4.0 y su "Manage Actions access" no incluía al repo, así que el `GITHUB_TOKEN` del workflow recibía `denied: permission_denied: write_package`. **Lección (hermana de la del PowerShell en 0.4.0):** un paquete GHCR preexistente NO hereda permiso para el token de Actions; hay que añadir el repo con rol Write una vez en los ajustes del paquete. **✅ RESUELTO 2026-07-10:** concedido el rol Write del repo al paquete y `gh run rerun 28969288702 --failed` → **0.5.0 (+ latest) publicadas en GHCR**, run verde entero (tests + build + smoke + push).
- ✅ **Fase 7 COMPLETA (7a + 7b) VALIDADA EN K3D (2026-07-09, en el PC de casa):** k3d instalado y clúster `sentinel` creado en esta máquina (cgroup v2 nativo — el fix #19 de WSL2 no hizo falta); imagen 0.5.0 construida en local (primera validación del Dockerfile por capas de la fase 6: build + smoke OK) e importada. **7a — SOPS+age** (decisión #39): secreto cifrado en el repo, secretGenerator retirado, y **demo de auto-reparación** (pod del poller borrado → sustituto Running en 1 s → gap-fill 92.945 velas). **7b — ArgoCD+KSOPS** (decisión #40): ArgoCD instalado, repo-server parcheado con KSOPS (clave age como Secret `sops-age`), dos Applications (`sentinel-local` raíz + `sentinel-local-secrets` overlay) en **Synced/Healthy** — el clúster se sincroniza solo desde git. **A partir de aquí, desplegar = hacer commit.** Bonus operativo: el compose local no tiene restart policy — tras reiniciar Docker, `docker compose -f docker-compose.dev.yml up -d` antes de esperar nada de la BD local.
- ✅ **Higiene de secretos RESUELTA (2026-07-10):** rotada la password del rol `grafana_ro` de Neon (`ALTER ROLE grafana_ro WITH PASSWORD '…'`), password nueva en el gestor. La vieja (que se pegó en un chat) queda invalidada.
- ✅ **Secreto prod cifrado (2026-07-10, commit `f73361f`):** `sentinel-env.prod.yaml` cifrado con SOPS+age (mismo Secret `sentinel-env`), con la `DATABASE_URL` del rol **owner** de Neon y la `grafana_ro` rotada. **Dato de infra: la base de datos de Neon se llama `neondb`** (la default), NO `sentinel` como sugería `.env.example` — los schemas gold/silver/bronze están en `neondb`; por eso `GRAFANA_DB_NAME=neondb` y `DATABASE_URL` termina en `/neondb` (host directo, sin `-pooler`). App y Grafana coherentes al mismo destino.
- ✅ **0.5.0 desplegada en k8s contra Neon (2026-07-13):** migración **0007** aplicada a Neon (`gold.distribution_cuts` creada; idempotente — solo corrió la 0007), poller 0.5.0 conectado a Neon (92.156 velas en el gap-fill), Grafana + 5 CronJobs. Imagen tirada **directa de GHCR** (público, sin `k3d image import`). **Cierra la cola de la 5¾ en clúster.** Arreglo de infra: el clúster se recreó con `k3d cluster create sentinel --api-port 6550` porque el puerto aleatorio de k3d (54122) caía en un rango excluido por WinNAT (`bind: forbidden by its access permissions`) tras reiniciar Docker — fijar un puerto bajo (<49152) lo evita para siempre. Gotcha: los primeros pods tras crear el clúster pueden fallar DNS unos segundos hasta que CoreDNS estabiliza (crash-only + restart lo cubre).
- ✅ **ArgoCD re-bootstrapeado gestionando Neon (2026-07-13, commit `27ca01b`):** al recrear el clúster se perdió el bootstrap de 7b, así que se rehízo — y se creó el **overlay prod que faltaba** (`deploy/gitops/prod/` + `application-sentinel-prod.yaml`, espejo del local apuntando a `sentinel-env.prod.yaml`). Applications `sentinel-prod` + `sentinel-prod-secrets` en **Synced/Healthy** contra Neon. **Un entorno por clúster** (no bootstrapear local y prod a la vez: gestionan los mismos recursos). Gotcha: el CRD `applicationsets` falla con `kubectl apply` normal (anotación >256KB) → instalar ArgoCD con `kubectl apply --server-side`.
- ✅ **Fase 8½ — backtest de la señal (2026-07-13, commits `d1e0b09`/`43fc747`):** migración **0008** (`gold.signal_entries` = cruces z≤−2 con `n_obs`≥126 vía LAG; `gold.signal_backtest` = resultado a +1/+3/+6m as-of por calendario con LATERAL; `gold.signal_backtest_summary` = tasas de acierto; función `gold.zscore_band` + columna en `cef_snapshot`) + panel en `cef-sentinel.json` (tabla de tasas de acierto + nota de caveats + columna "Signal" con número y banda coloreada, mismo patrón que "Status"). Aplicada a Neon vía runner de la imagen 0.5.0 (`docker run -v db/migrations image migrate`; uv no está en PATH local). **Resultado validado pero NO robusto:** 43 señales en 12 tickers; +1m reversión 86,8% / precio +4,2%, +3m 76%, +6m sin madurar — pero **la mitad (22) son del sell-off de crédito de 2026-03** (los 12 tickers a la vez): señales correlacionadas, muestra efectiva pequeña. Sesgo de agrupamiento documentado en el propio panel; la muestra crece con la serie. Decisión de presentación: número crudo + banda legible juntos, cortes en gold (semántica en gold, Grafana solo pinta).
- 🔄 **Fase 9 (observabilidad) EN CURSO — commits 1-2 de 4, validados OFFLINE (2026-07-13):** Prometheus "a pelo" (Deployment + PVC, sin Operator) + `/metrics` en el poller. Decisiones **41-43** abajo. Hecho y validado sin clúster: `kubectl kustomize .` renderiza limpio, **42 tests verdes** (el import de `prometheus_client` no rompe nada) y el endpoint `/metrics` sirviendo las 10 series `sentinel_*`. **Pendiente:** Pushgateway para los CronJobs (commit 3), datasource Prometheus + paneles que sustituyen el panel-hueco (commit 4), hornear la **0.6.0** (la 0.5.0 en clúster no lleva `prometheus_client` → el scrape del poller espera a esa imagen; `uv.lock` y `pyproject` ya coherentes, el CI `--locked` pasaría) y **validación in-cluster** (PVC bound, `/targets`, fsGroup) — el k3d estaba caído. Confirmar el tag `prom/prometheus:v3.5.0` al levantarlo (elegido LTS, sin pull de prueba).
- ➡️ **Siguiente:** cerrar la fase **9** (Pushgateway + paneles Grafana + 0.6.0 + validación in-cluster). Después: fase **8** (alertas Telegram) **aparcada por preferencia del usuario**, **10** calidad de dato, **11** Helm chart + README final. Despliegue en **Ubuntu** pendiente de acceso al servidor (la prueba de fuego de la portabilidad). **Para retomar en OTRA máquina: copiar la clave privada age** (`%APPDATA%\sops\age\keys.txt`) a la ruta equivalente allí — sin ella no se descifra nada.

## Decisiones (resuelven las 7 dudas abiertas del brief)

1. **Base de datos → Neon** (Postgres gestionado, free tier). Estado externalizado: cualquier clúster escribe contra la misma base y sigue disponible con las máquinas apagadas. Reversible a Postgres en k3s (vía connection string en un Secret) si algún día hay un servidor 24/7.
2. **Modelo → Postgres plano** (no TimescaleDB). El volumen (~1,5M filas/año) está muy por debajo de donde Timescale aporta; el medallón ya cubre rollups (capa gold) y retención (poda de bronze). Escape futuro aditivo: particionado nativo de Postgres → hypertables solo si se migra a un Postgres con Timescale.
3. **NAV → CEFConnect (primario) + yfinance (cross-check).** Un solo scraper para todo el universo, tras una abstracción `NAVProvider`; webs de gestoras como fallback documentado. Etiquetado por frescura: `definitivo` / `estimado` / `rancio`. Huecos tolerados y documentados (no se inventa dato). Las alertas conocen la confianza del NAV. *(Endpoint concreto pendiente de spike de validación.)*
4. **Intradía → polling yfinance** (no websocket). Cadencia config-driven ~60-90s (no 30s: el dato ya viene ~15 min retrasado). Patrón de backfill idempotente: tick en vivo + gap-fill al arrancar; upsert por `(ticker, ts)`; over-fetch + dedup. Finnhub, opcional no prioritario.
5. **Imagen → una sola + dispatcher CLI** (`sentinel poller` / `nav` / `alerts`). Monorepo de una app; lockstep del mismo SHA contra el esquema compartido; K8s elige la cara vía `command`/`args`. Secretos nunca horneados en la imagen.
6. **Nombre → `k8s-market-sentinel`** + disclaimer fuerte y completo (pura y fuertemente formativo/educativo, no consejo de inversión) + licencia MIT + línea de disclaimer en el pie de las alertas.
7. **Secretos → por fases (PROVISIONAL, revisar en GitOps).** Secrets a pelo (`.env` en `.gitignore`) en fases 1-3 → **SOPS + age** al cablear GitOps (clave que viaja entre k3s/k3d/local, mejor que Sealed Secrets para la portabilidad). Vault/ESO fuera de alcance.

## Decisiones técnicas de Fase 1 (2026-07-06)

Tomadas al implementar la fundación; todas con puerta de escape documentada:

1. **Migraciones → SQL numerado + runner propio** (`sentinel migrate`, tabla `public.schema_migrations`). No Alembic: su fuerte es autogenerar diffs desde un ORM que no usamos; ficheros SQL planos son 100% transparentes. Regla: una migración aplicada nunca se edita — los cambios son migraciones nuevas.
2. **Capas como schemas de Postgres** (`bronze.`, `silver.`, `gold.`): namespace explícito en cada query y permisos por capa si hicieran falta.
3. **Bronze = una sola tabla genérica** (`bronze.raw_fetches`, jsonb, append-only). Lo que varía entre fuentes es la forma del payload → jsonb; nunca se actualiza ni deduplica (es la auditoría que permite re-procesar silver).
4. **Silver separa grano diario e intradía** (`prices_daily` con `date`, `prices_intraday` con `timestamptz` UTC): claves naturales distintas, retención futura distinta; fusionarlas obligaría a inventar timestamps falsos. La PK natural de cada tabla ES el contrato del upsert idempotente.
5. **Precios SIN ajustar** (`auto_adjust=False`): el descuento compara el precio real de pantalla contra el NAV publicado; los precios ajustados por dividendos reescriben la historia (y los CEFs de crédito reparten mucho).
6. **Gold = vistas, no tablas**: nunca desactualizadas, cero orquestación. Escape: `MATERIALIZED VIEW` + refresh en CronJob si algún día pesa (las queries de dashboards no cambiarían).
7. **Sin ORM ni framework de config**: psycopg3 a pelo, SQL visible, `argparse` para el dispatcher. El proyecto es SQL-céntrico y formativo; menos magia = más comprensión.
8. **Commit por ticker** en la ingesta: si el proceso muere a mitad del universo, lo ingerido queda a salvo y la siguiente ejecución se autorrepara. Un ticker que falla no tumba a los demás; el exit code del CLI refleja fallos (los Jobs de K8s se enteran por ahí).
9. **Tests**: lógica pura (ventana de backfill, parseo) con unit tests; el upsert idempotente se valida contra Postgres real (docker-compose.dev.yml), no con mocks.
10. **Numerador del Buffett → `^W5000` vía yfinance** (decisión de Michael, 2026-07-06). WILL5000PR fue **eliminada de FRED** (Wilshire dejó de publicar allí en 2023; la API devuelve 400). Alternativas evaluadas: ^W5000 diario (proxy del market cap, reutiliza el ingestor de precios) vs serie Z.1 trimestral oficial (BOGZ1LM893064105Q, ~2,5 meses de retraso). Se eligió frescura diaria; la vista gold lee de `silver.prices_daily` (migración 0004).
11. **Ingestor FRED**: mismo patrón que precios con solape de **30 días** (FRED revisa hacia atrás — comprobado en vivo: el segundo run recogió una revisión del PIB de Q4-2025). Los huecos `value="."` se omiten. `urllib` de la stdlib, sin dependencia nueva.
12. **FX BCE → `api.frankfurter.dev/v1`** (el dominio `.app` migró: hace 301 y urllib acababa en 403). Serie `EURUSD_ECB` en `silver.macro_series`: es el **fixing oficial** (~16:00 CET, definitivo) contra el que se evalúa la banda 1,10–1,20; el `EURUSD=X` de yfinance es cotización de mercado, otra cosa. Lección reutilizable: mandar **User-Agent propio** — los CDN bloquean el de Python por defecto.
13. **NAV → CEFConnect `api/v3/pricinghistory/{ticker}/1Y` (spike #3 RESUELTO).** Hallazgos: el periodo `1Y` es el único con datos **diarios** (~245 puntos); 1M/3M/5Y vienen muestreados para gráficos. Acepta nuestro UA honesto. El payload trae `NAVTicker` (`XWDIX`...) para el cross-check con yfinance — verificado que funciona. Divergencia del patrón: la API va por periodos, no rangos → siempre se pide 1Y y se upsertea todo (backfill máximo = 1 año; huecos anteriores tolerados, como preveía el brief). Solo se guarda `NAVData`: el descuento es métrica NUESTRA (gold), el suyo se descarta — y aún así ambos coinciden al céntimo (validación externa).
14. *(Fase 2, 2026-07-06)* **Imagen: `python:3.13-slim`, un solo stage, non-root.** Sin multi-stage porque todas las deps llegan como wheels precompilados — no hay toolchain que separar (puerta de escape documentada en el Dockerfile). slim y no alpine: musl obligaría a compilar pandas. Usuario `sentinel` (uid 1000) para que K8s pueda exigir `runAsNonRoot`. `ENTRYPOINT ["sentinel"]` + `CMD ["--help"]`: K8s elige la cara vía `args:`. Cache mount de BuildKit para pip. 481 MB (pandas/numpy pesan; asumido para una imagen de datos).
15. *(Fase 2)* **install EDITABLE DENTRO de la imagen**: el paquete queda en `/app/src` y `REPO_ROOT=/app` resuelve `config/` y `db/migrations` en las mismas rutas relativas que en desarrollo. Con install normal el código iría a site-packages y esas rutas se romperían. Escape futuro: package-data con `importlib.resources`. *(Fase 6, decisión #36: el mecanismo pasó de `pip install -e .` a `uv sync --locked`, que también instala el proyecto editable — el principio y las rutas se conservan intactos.)*
16. *(Fase 2)* **Secretos jamás en la imagen**: `.env` excluido por `.dockerignore` (además de `.gitignore`). Local: `docker run --env-file .env`; K8s: Secret → env vars. Validado end-to-end: la imagen ejecutó `migrate`, `ingest-fx` e `ingest-prices` contra Neon.
17. *(Fase 3, 2026-07-07)* **kustomize con generadores, kustomization.yaml en la RAÍZ del repo.** El ConfigMap del universo se genera desde `config/tickers.yaml` y el Secret desde `.env` (una sola fuente de verdad; secretos fuera del repo). En la raíz porque kustomize se niega a leer ficheros fuera de su directorio (LoadRestrictionsRootOnly). `disableNameSuffixHash: true`: los consumidores son Jobs que nacen en cada ejecución (el rollout que motiva el hash no aplica) y `job-migrate.yaml` puede referenciar nombres estables. Despliegue = `kubectl apply -k .`
18. *(Fase 3)* **CronJobs con `timeZone: "Europe/Madrid"`** (los horarios viven en mi zona, DST incluido), `concurrencyPolicy: Forbid`, `startingDeadlineSeconds: 3600` — una ejecución perdida por tener la máquina apagada NO se recupera: el backfill idempotente se pone al día en la siguiente (el diseño del brief hecho spec de K8s). Migraciones = Job puntual con `generateName` (se lanza con `kubectl create`, no forma parte del estado deseado).
19. *(Fase 3)* **Dos lecciones de infra aprendidas en vivo:** (a) el kubelet de K8s ≥1.35 **se niega a arrancar sobre cgroup v1** — la VM de WSL2 lo usa por defecto; arreglo: `C:\Users\Felipe\.wslconfig` con `kernelCommandLine = cgroup_no_v1=all` + `wsl --shutdown` (el Ubuntu del servidor no lo necesitará). (b) Con `runAsNonRoot: true`, el `USER` de la imagen debe ser **numérico** (`USER 1000`): el kubelet no lee `/etc/passwd` de la imagen y con un nombre falla con `CreateContainerConfigError`.

## Decisiones técnicas de Fase 4 — poller intradía (2026-07-07)

20. **Calendario de mercado → `exchange_calendars` (XNYS)**, envuelta en `market_hours.py` (el resto del código nunca importa la librería: puerta de escape). Resuelve las tres trampas que hacen inviable el "if 15:30-22:00 CET": DST transatlántico (~3 semanas/año el desfase NY-Madrid es 5h, no 6h — el horario real es 9:30-16:00 America/New_York), festivos con reglas móviles/traslados, y medias sesiones (cierre 13:00 NY). Los tests cubren exactamente esas tres trampas. Las alertas (fase 6) reutilizarán esta interfaz.
21. **Anatomía crash-only del bucle** (`poller.py`): gap-fill al arrancar hace que morir sea barato (el camino de recuperación ES el arranque normal); sueño interrumpible con `threading.Event` + SIGTERM (nunca `time.sleep`: la señal despierta al instante y muere limpio dentro de la gracia de K8s); siestas de ≤15 min con latido cuando cierra el mercado; tick agendado por reloj de pared (sin deriva). Errores por tick → log y siguiente; **30 fallos seguidos → crash a conciencia** (mejor reinicio que zombi). El gap-fill fallido NO crashea: reintentar en bucle de reinicios amplificaría un 429 (cada reinicio = otra petición 7d).
22. **Ingesta intradía = velas 1m en UNA petición batch** (`ingest/intraday.py`, decisión 4.3): rate-limit ×24 mejor que pedir por ticker; timestamps DEL MERCADO (la PK (ticker, ts) deduplica de verdad tras reinicios); tick normal y gap-fill son la misma función con distinta ventana ("1d" vs "7d" — el máximo 1m de yfinance; huecos más viejos, irreparables y asumidos). **Bronze solo en el gap-fill**, desviación consciente: guardar 24 DataFrames casi idénticos cada minuto inflaría el free tier repitiendo el 99%.
23. **Conexión a BD POR TICK, no persistente**: Neon autosuspende con la inactividad — una conexión abierta el fin de semana (65h) estaría muerta el lunes. Un handshake cada ~75s no cuesta nada y deja a Neon dormir fuera de horario (amable con el free tier). Cadencia config-driven: `defaults.poll_interval_seconds: 75` en tickers.yaml.
24. **Deployment con `replicas: 1` y `strategy: Recreate`** (rolling = dos pollers unos segundos; preferible un hueco que cubre el gap-fill). **Liveness por fichero-latido + exec probe** (`find /tmp/heartbeat -newermt "-20 minutes"`; 15 de siesta máxima + margen): si el bucle se cuelga sin morir, el fichero envejece y K8s reinicia — el zombi se cura solo. **Sin readinessProbe a conciencia** (el poller no recibe tráfico; ponerla sería cargo cult). Escape futuro: cuando Prometheus (fase 7) pida `/metrics`, la probe podrá migrar a HTTP.

## Decisiones técnicas de Fase 5 — gold para dashboards + Grafana (2026-07-07)

25. **Grafana in-cluster, SIN PVC, 100% aprovisionado como código** (lo que pedía el brief #7): datasource y dashboards JSON viven en el repo y entran por ConfigMaps generados por kustomize; el pod es desechable (crash-only aplicado al visor). El flujo de edición ES GitOps: UI como borrador → Export JSON → commit → apply (y `allowUiUpdates: false` lo fuerza). Descartados: PVC (segunda fuente de verdad; el trofeo stateful llega con Prometheus en fase 7) y Grafana Cloud (saca del clúster la pieza a demostrar). El provider recarga dashboards solo (~1 min tras el apply) sin reiniciar el pod; cambiar el datasource sí pide reinicio (raro).
26. **La semántica del dato vive en gold; Grafana solo pinta.** Migración 0005: `gold.discount_intraday` (as-of join LATERAL precio-del-minuto vs último NAV conocido — mismo patrón que el PIB en buffett_indicator) con etiquetado por frescura en SQL: `estimado` ≤4 días naturales (absorbe findes: el lunes usa el NAV del viernes y es sano), `rancio` >4 (scraper roto). Y `gold.cef_snapshot`: la fila "ahora" por CEF (DISTINCT ON) con `premium_discount` CON SIGNO + columna `estado` (prima/descuento/par — los PIMCO cotizan con primas persistentes y se ven el día uno). Los UMBRALES personales (−5%/−7%, banda 1,10-1,20) NO van en SQL: en fase 5 son líneas de referencia en Grafana (display), en fase 6 reglas declarativas en ConfigMap (acción). Duplicación consciente y documentada.
27. **Categoría `stocks` en el universo** (config-driven puro: editar YAML y re-aplicar): top-10 USA + top-10 Europa. Las europeas vía su cotización USA (ADR): viven en el calendario XNYS del poller (una acción de París solo se vería 2h/día) y todo queda en USD. Sin NAV: jamás entran en el pipeline de descuento (`nav_tickers` no las incluye). Campo opcional en config (`raw.get("stocks", [])`).
28. **Grafana entra a Neon con rol de SOLO LECTURA `grafana_ro`** (mínimo privilegio: es la única pieza con UI expuesta y ejecuta SQL editable clicando). GRANT SELECT sobre gold, silver y bronze (bronze añadido en el smoke test local: el panel "Bronze fetch log" del dashboard de operación lo lee — sigue siendo solo-lectura) + `ALTER DEFAULT PRIVILEGES` para que las vistas futuras hereden solas. Otro hallazgo del smoke: `sslmode` configurable por env (`GRAFANA_DB_SSLMODE`) — Neon exige `require`, el Postgres local del compose necesita `disable`. La creación del rol es paso manual documentado (la password no puede ir en una migración: las migraciones van a Git). Credenciales explícitas por `secretKeyRef` (5 claves), NO `envFrom` del Secret entero: Grafana no debe ver DATABASE_URL(owner) ni FRED_API_KEY.
29. **Dashboards** (títulos en inglés — los verán recruiters; acceso por NodePort 30300, cambiable): "CEF Sentinel" (semáforos macro como stat tiles, LA tabla del universo ordenada por z-score con color divergente verde=descuento/rojo=prima, series diaria+z-score con líneas de referencia, intradía titulado ESTIMATED, watchlist al pie con semántica estándar verde=sube) y "Pipeline Health" (frescura por fuente con umbrales que toleran findes, actividad por día en escala log, NAVs rancios, log de bronze, y panel de texto documentando el hueco que llenará Prometheus). Sin tartas, sin gauges, sin doble eje. Grafana SÍ lleva readinessProbe además de liveness — recibe tráfico, contraste deliberado con el poller. docker-compose.dev.yml levanta el MISMO Grafana en local para iterar dashboards.

## Decisiones técnicas de la mini-fase yield (2026-07-07/08)

30. **Distribuciones → columna `Dividends` del MISMO `history()` de yfinance** (`actions=True`): sin endpoint nuevo ni scraper nuevo — la fuente ya validada de precios trae las distribuciones (días normales valen 0, los ex-date llevan el importe; se filtra > 0). Grano = fecha ex-dividendo, PK `(ticker, ex_date)` como contrato del upsert. Solape de **30 días** (el macro, no el de precios): una distribución no se revisa como una vela, pero los anuncios llegan con retraso al feed. Solo CEFs (`universe.nav_tickers`): en un CEF la distribución ES la tesis; dividendos de stocks del watchlist = otra decisión futura (escape: ampliar el set en config).
31. **`gold.yield_ttm` con DOS denominadores y DOS numeradores, a conciencia.** Denominadores: sobre **precio** (lo que cobra quien compra hoy) y sobre **NAV** (lo que la cartera debe generar — sostenibilidad); la diferencia entre ambos ES el descuento (FSCO: 15,8% vs 10,9%). Numeradores: **TTM** (lo realmente pagado en 12m, mira atrás) y **current** (última distribución anualizada al ritmo de pagos `n_dist_ttm`, que además delata la cadencia: ~12 mensual, ~4 trimestral). Señal de dominio: `current < TTM` ⇒ **recorte reciente** — el evento que históricamente precede a las explosiones de descuento y la alerta clave de la fase 6. Detectó 4 recortes reales el primer día (BST -29%, FSCO -10%).
32. **Bronze pragmático en distribuciones** (misma desviación documentada que el intradía): se guarda solo la serie de dividendos de la ventana, no el OHLC completo — ese ya entra a bronze por el ingestor de precios y duplicarlo solo infla el free tier.
33. **Panel de yield: el color NO premia el yield alto.** Un 20% puede ser distress, no regalo — pintar de verde "más yield" sería mentir. El gradiente va solo en la tendencia de la distribución (current vs TTM), donde rojo = recortando SÍ es semántica honesta; la descripción del panel remite al z-score de la tabla de arriba. Tabla separada del universo (grano distinto: distribuciones + último precio + último NAV), watchlists desplazadas abajo. CronJob a las 23:15 (precios va a las 23:05; misma fuente, 10 min de separación para no solapar peticiones a Yahoo ni conexiones a Neon).

## Decisiones técnicas de la fase 5¾ — flecos (2026-07-08)

34. **`intraday_exclude` en config (clave opcional en `defaults`) + property `price_tickers_intraday`.** ^W5000 se ingesta en DIARIO (numerador del Buffett) pero Yahoo no da velas 1m de índices → cada tick del poller logueaba 2 líneas de error por él (inofensivo pero sucio). El carril diario sigue usando `price_tickers` (los quiere todos); solo el intradía filtra. Config-driven puro (añadir/quitar en el YAML, el código no cambia) y degrada a lista vacía sin la clave (compat hacia atrás con ConfigMaps viejos, como `stocks`). 4 tests nuevos (`test_config.py`).
35. **`gold.distribution_cuts` (migración 0007) para las annotations de recortes.** La tesis del proyecto es que el recorte precede a la ampliación del descuento; esta vista aísla los recortes para pintarlos como líneas rojas sobre la serie de descuento en el deep-dive (variable `$cef` que ya existía) — se VE la relación temporal. Semántica en gold, Grafana solo pinta (#26). **Definición robusta a conciencia:** un recorte es un pago por debajo (>2%, filtra redondeo) de LOS DOS pagos anteriores, no solo del inmediato. Motivo empírico: con solo el inmediato, los CEFs de contraste (ADX, BST...) que reparten una distribución ESPECIAL de fin de año generaban falsos recortes del -90% (el pago normal siguiente vs la especial) que tapaban los recortes reales (FSCO -14,7%); exigir que quede por debajo también del pago pre-especial los descarta y conserva los graduales. Limitación acotada y documentada: un recorte real justo tras una especial podría no marcarse (raro). Los default privileges de la #28 cubrieron `grafana_ro` sobre la vista nueva sin tocar nada. Datasource `neon-sentinel`, annotation aprovisionada en el JSON (built-in de Grafana conservada).

## Decisiones de la fase 6 — CI/CD (2026-07-08)

> Acordadas en sesión y **ya implementadas** (commits c2e914f/c557d82 — ver Estado general para el detalle y el bloqueo de permisos de GHCR).

36. **Lock de dependencias → `uv.lock` + `uv sync` en un Dockerfile POR CAPAS.** El build hoy instala "lo último" en cada `docker build` (no reproducible). Se clava con `uv lock` (hashes criptográficos) y el Dockerfile se reestructura: copiar `pyproject.toml`+`uv.lock` PRIMERO y `uv sync --no-install-project` (capa de deps cacheada, solo cambia si cambian las deps), LUEGO copiar el código y `uv sync` (instala el proyecto editable — preserva la decisión #15: `REPO_ROOT=/app` resuelve `config/` y `db/migrations`). Se mantiene `USER 1000` (#14/#19). La robustez de "el arranque nunca falla" NO viene de la herramienta de lock (requirements.txt daría lo mismo) sino de tres cosas ortogonales: deps con hash + Dockerfile por capas (deps antes que código) + smoke test en CI antes del push. Se eligió uv.lock por ser el artefacto nativo y moderno.
37. **CI en GitHub Actions con trigger de imagen TAG-BASED.** Dos jobs: `test` (en cada push y PR → `pytest`, que es 100% lógica pura, sin Postgres) y `build-push` (solo al empujar un tag `v*`, p.ej. `v0.5.0` → build + push a GHCR como `:X.Y.Z`+`:latest`). Tag-based y no build-continuo-en-main: releases intencionadas, una imagen por versión, GHCR limpio (elección de Michael: "sin commits constantes"), y amigo de ArgoCD (fase 7). **Autenticación con el `GITHUB_TOKEN` que Actions provee solo** (`permissions: packages: write`): adiós PAT manual y adiós al pipe de PowerShell que corrompía el token en la saga 0.4.0 — el problema desaparece de raíz.
38. *(implementada por Michael el 2026-07-08 — ver Estado general)* **El bump a 0.5.0 es PARTE de ejecutar la fase 6, no un paso previo.** La imagen 0.5.0 (con migración 0007 + `intraday_exclude`) la construye el CI al etiquetar `v0.5.0`. Por eso NO se sube la versión en `pyproject.toml`/`__init__.py`/`kustomization.yaml`/`job-migrate.yaml` hasta implementar el workflow: hacerlo antes deja el repo inconsistente (número que no corresponde a ninguna imagen publicada). Tarea futura opcional: single-source de la versión (reducir los 4 sitios) derivando la etiqueta de imagen del tag de git.

## Decisiones de la fase 7a — secretos SOPS + age (2026-07-09)

39. **Los secretos viven CIFRADOS en el repo (`deploy/secrets/`), y el único secreto fuera de git es la clave privada age.** Cierra la decisión provisional #7 tal y como se anticipó. Mecánica: `.sops.yaml` en la raíz declara la regla (todo YAML bajo `deploy/secrets/` se cifra para la clave age del proyecto; `encrypted_regex: ^(data|stringData)$` deja los metadatos legibles — kustomize/ArgoCD los necesitan y los diffs de git tienen sentido). El `secretGenerator` desde `.env` se RETIRA del kustomization: el `.env` queda como comodidad de desarrollo local (compose/CLI), ya no es la fuente del clúster. **Un secreto cifrado POR ENTORNO** (`sentinel-env.local.yaml` = Postgres del compose vía `host.k3d.internal`; `sentinel-env.prod.yaml` = Neon en la base `neondb`, cifrado el 2026-07-10): mismo nombre de Secret (`sentinel-env`), los manifests no cambian ni una línea. Flujo pre-ArgoCD: `sops -d deploy/secrets/<entorno>.yaml | kubectl apply -f -`; con ArgoCD (7b), KSOPS descifra al sincronizar. La clave privada vive en la ruta estándar de SOPS (`%APPDATA%\sops\age\keys.txt` en Windows, `~/.config/sops/age/keys.txt` en Linux) y viaja UNA vez por máquina/clúster — de "sincronizar N secretos a mano" a "colocar 1 fichero una vez". vs Sealed Secrets: su clave vive en el controlador de UN clúster (re-cifrar por clúster); la de age viaja contigo — portabilidad multi-clúster (k3d casa / k3s Ubuntu), el criterio rector. Gotcha de Windows aprendido: `path_regex` debe ser `deploy[/\\]secrets[/\\]` porque sops evalúa la ruta con backslashes. **Validado end-to-end en k3d en esta máquina (2026-07-09):** clúster nuevo (cgroup v2 nativo, sin el fix #19), despliegue completo con el flujo nuevo, y demo de auto-reparación: pod del poller eliminado → sustituto Running en 1 s → su gap-fill upserteó 92.945 velas que el pod anterior no pudo (la BD estaba caída cuando arrancó): crash-only + backfill idempotente convertidos en garantía por la plataforma. **Raíz de confianza — por qué UNA clave portada y no "todo en la nube" (nota 2026-07-10):** no se puede eliminar el ancla, solo reubicarla (*turtles all the way down*): si se cifra la clave age con otra cosa, esa otra cosa pasa a ser el ancla. La criptografía siempre "toca fondo" en una raíz de confianza que ya no protege otro secreto propio. Se eligió age = ancla **portable y provider-agnostic** precisamente por eso (vs Sealed Secrets, atada a un clúster; vs cloud KMS, atada a un proveedor). Matiz operativo clave: el hogar canónico de la clave age NO es "un PC" sino el **gestor de contraseñas** (nube); el `keys.txt` del disco es una materialización temporal que se recrea bajo demanda en cualquier máquina — así el ancla real es la **contraseña maestra** (en la cabeza + MFA, revocable), no una máquina física. Alternativa cloud KMS (AWS/GCP/Azure): movería el ancla a la identidad IAM (centralizado, auditable) pero **ataría el proyecto a un proveedor** → choca con la portabilidad multi-clúster, el criterio rector. Trade-off deliberado, no mejora gratis. Incluso el clúster tiene su ancla (la clave age como Secret `sops-age` en etcd, protegido por el cifrado en reposo + RBAC): la raíz de confianza no desaparece, solo cambia de forma.

## Decisiones de la fase 7b — GitOps con ArgoCD + KSOPS (2026-07-09)

40. **ArgoCD sincroniza el clúster desde git; KSOPS descifra los secretos en el repo-server; el despliegue se parte en DOS Applications.** El agente vive en el clúster (bootstrap manual una vez: `kubectl apply` de los manifests de ArgoCD, no se auto-gestiona desde cero — huevo/gallina) y reconcilia continuamente contra `main`. **KSOPS** (plugin de kustomize) es el puente SOPS↔ArgoCD: se parchea el `argocd-repo-server` (`deploy/gitops/argocd/repo-server-ksops-patch.yaml`) con un initContainer que instala el binario `ksops` + la clave age montada desde el Secret `sops-age` (creado a mano, el único fuera de git) vía `SOPS_AGE_KEY_FILE`, y `argocd-cm` habilita `kustomize.buildOptions: --enable-alpha-plugins --enable-exec`. El descifrado ocurre dentro del repo-server; el claro nunca toca disco. **DOS Applications a conciencia** (`deploy/gitops/argocd/application-sentinel-local.yaml`): `sentinel-local` apunta a la raíz (`.`, la app completa, SIN plugins → el `kubectl apply -k .` del humano sigue intacto) y `sentinel-local-secrets` apunta a `deploy/gitops/local` (solo el Secret vía el generator KSOPS). El reparto **rompe un ciclo de kustomize**: un overlay que hace `resources: [../../..]` referencia su propio ANCESTRO (la raíz, que por la decisión #17 tiene que vivir arriba para que los generadores lean `config/`), y kustomize lo prohíbe ("cycle detected"). Ambas con `automated + prune + selfHeal`: lo que se va de git se va del clúster, y la deriva manual se revierte sola — la auto-reparación aplicada al DESPLIEGUE, no solo a los pods. Lección de infra (hermana de las del PowerShell y el cgroup): la imagen de KSOPS pasó a **distroless** en la v4.4.0 (sin `/bin/sh`), lo que rompe la receta clásica de initContainer con shell; se usa el subcomando `ksops install` de la v4.5.1. **Validado en k3d (2026-07-09):** las dos Applications en **Synced/Healthy**, el Secret con sus 10 claves salido del descifrado KSOPS, y ArgoCD gestionando namespace + 5 CronJobs + poller + Grafana. A partir de aquí, desplegar = hacer commit.

## Decisiones de la fase 9 — observabilidad: Prometheus + PVC (2026-07-13)

> Commits 1-2 de 4 implementados y validados offline (el clúster estaba caído);
> Pushgateway (commit 3) y paneles Grafana (commit 4) pendientes. Ver Estado general.

41. **Prometheus "a pelo" (Deployment + ConfigMap + PVC), NO kube-prometheus-stack ni Operator; Deployment+PVC y no StatefulSet.** El scrape config vive como fichero versionado (`deploy/prometheus/prometheus.yml`) y el ConfigMap se GENERA con kustomize desde ahí — mismo patrón config-driven que la Grafana aprovisionada (#25). Sin Operator/stack: traería su propia Grafana (choca con la nuestra), CRDs y ServiceMonitors (magia), y va contra el criterio SQL-sin-ORM / kustomize-sin-Helm; escape documentado = el Operator si el set de targets crece. **`strategy: Recreate` + PVC standalone, no StatefulSet** (elección de Michael): a 1 réplica el StatefulSet (volumeClaimTemplates + Service headless) no aporta función, y Recreate evita que el pod nuevo pelee con el viejo por el PVC ReadWriteOnce (mismo razonamiento que el poller, #24); escape = StatefulSet si algún día se replica/shardea. **El PVC es el PRIMER estado cluster-local del proyecto — "el trofeo stateful":** todo lo demás se externaliza a Neon; la TSDB no, y por eso NO sobrevive a un teardown del clúster (métricas operativas, aceptable). StorageClass default (local-path en k3d y en el k3s del Ubuntu) = portable, el criterio rector; retención `15d` acota el PVC para que no se llene solo. **Gotcha (hermano del uid-1000 numérico de #19):** `prom/prometheus` corre como `nobody` (uid 65534) y un PVC nace propiedad de root → sin `securityContext.fsGroup: 65534` no puede escribir la TSDB y entra en CrashLoop.

42. **El poller expone `/metrics` (modelo pull); la liveness NO migra a HTTP.** El poller es un proceso VIVO → Prometheus lo scrapea: `prometheus_client` levanta un HTTP server en un hilo daemon, arrancado en la CAPA DE PROCESO (`cli.py`, junto a las señales) y NO en `poller.run()` — abrir un socket es efecto de proceso, así el bucle se sigue testeando sin puertos (la misma separación que ya aislaba las señales de `run()`). Siete métricas: `sentinel_poller_ticks_total` / `_tick_failures_total` / `_candles_upserted_total` (counters) y `_consecutive_failures` / `_last_tick_timestamp_seconds` / `sentinel_market_open` / `_gapfill_candles` (gauges). **La liveness se queda en el fichero-latido y NO migra al `/metrics`, aunque la #24 lo dejó como puerta de escape:** ese endpoint lo sirve un hilo daemon aparte y respondería 200 aunque el BUCLE esté colgado → peor detector de zombis que el latido, que sí está acoplado al progreso del bucle. Una puerta de escape que, mirada de cerca, era trampa. Corolario: el poller ahora SÍ recibe tráfico (el scrape) → gana un Service ClusterIP `poller-metrics`, matizando la #24 ("no recibía tráfico de nadie"); pero sigue sin readinessProbe (1 réplica; un target caído lo marca Prometheus, no hay enrutado de Service que decidir).

43. **Scrape estático contra Services + Pushgateway para los CronJobs; alcance = métricas de app.** Scrape ESTÁTICO contra los Services in-cluster, no `kubernetes_sd_configs`: para 2-3 targets fijos el relabeling por anotaciones es cargo cult y dos jobs estáticos se leen de un vistazo; escape = k8s-SD si el set se vuelve dinámico. Los **CronJobs mueren antes de que Prometheus los scrapee** → **Pushgateway** (el patrón canónico de métricas de batch, avalado por los propios docs de Prometheus): el job empuja al salir. El push irá en `_print_ingest_summary` (el único embudo por el que pasan los 5 ingestores), **gateado por env `PUSHGATEWAY_URL`** — ausente en local = no empuja, degrada solo (mismo patrón que `intraday_exclude`, #34). *(Pushgateway = commit 3, pendiente.)* **Alcance de la fase (elección de Michael): SOLO métricas de app** (poller + ingestores). CPU/mem/reinicios de contenedor necesitan cAdvisor (RBAC `nodes/metrics` + bearer token) + kube-state-metrics (otro deployment) → hueco menor, se re-documenta en el panel y no se cargo-cultea ahora; escape = añadir esos scrapes cuando compensen.

## Roadmap con estado (redibujado 2026-07-08 bajo la lente "arquitectura portable primero")

Las fases 6-7 originales del brief se reordenan y amplían: CI/CD y GitOps se adelantan (todo lo posterior viaja sobre ellos y desaparece el despliegue manual), el dominio (backtest, score) va al final como escaparate. El razonamiento: no vamos a ser "el mejor servicio financiero" — vamos a hacer la mejor arquitectura posible, portable y aplicable a otros campos, con la tesis CEF como ejemplo vivo que valga la pena.

| Fase | Descripción | Estado |
|---|---|---|
| 0 | Decisiones de arquitectura (7 dudas abiertas) | ✅ Hecho |
| 1 | Fundación: esquema bronze/silver/gold + 4 ingestores (precios, FRED, FX, NAV) con backfill idempotente, fuentes validadas | ✅ Hecho (2026-07-06) |
| 2 | Contenerización: Dockerfile, `.env.example`, secrets fuera del repo | ✅ Hecho (2026-07-06) |
| 3 | k3s en Ubuntu: namespace, Secrets, ConfigMap de tickers, CronJobs del carril lento | ✅ Validado en k3d local (2026-07-07); Ubuntu pendiente de acceso |
| 4 | Poller intradía: Deployment con lógica de horario de mercado + festivos USA | ✅ Hecho y validado en local + k3d (2026-07-07, mercado abierto) |
| 5 | Capa gold + Grafana: queries de descuento/z-score/Buffett, dashboards provisionados | ✅ Hecho y validado contra Neon, local + k3d (2026-07-07) |
| 5½ | Mini-fase yield: distribuciones + `gold.yield_ttm` + panel + CronJob (imagen 0.4.0) | ✅ Hecho y validado en k3d contra Neon (2026-07-07/08) |
| 5¾ | Flecos: columnas yield + column guide en la tabla del universo, `intraday_exclude` para ^W5000, annotations de recortes (`gold.distribution_cuts`, migración 0007) sobre la serie de descuento (la variable `$cef` del deep-dive ya existía) | ✅ Hecho local 2026-07-08; desplegado en k8s contra Neon 2026-07-13 con la 0.5.0 (migración 0007 + `intraday_exclude`) |
| 6 | CI/CD: GitHub Actions (tests + build + push a GHCR) + lock de dependencias (`uv lock`) — adelantada para que todas las fases siguientes viajen sobre ella | ✅ Completa (2026-07-10): 0.5.0 + latest publicadas en GHCR, pipeline verde entero |
| 7a | Secretos GitOps-ready: SOPS + age, secreto cifrado por entorno en `deploy/secrets/`, sin secretGenerator (decisión #39) | ✅ Validado en k3d (2026-07-09); secreto prod (Neon, base `neondb`) cifrado 2026-07-10 |
| 7b | ArgoCD + KSOPS: el clúster se sincroniza solo desde el repo — el "auto-despliegue" es verdad literal (decisión #40) | ✅ Validado en k3d (2026-07-09); re-bootstrapeado contra Neon 2026-07-13 con overlay prod nuevo, Synced/Healthy |
| 8 | Motor de alertas Telegram: reglas declarativas en ConfigMap + digest diario de cierre + alertas puntuales (z-score, recorte `current<TTM`, banda EUR/USD, NAV rancio como alerta de operación). Documentar el descarte de Grafana Alerting | ⬜ |
| 8½ | Mini-fase backtest de la señal: `gold.signal_backtest` (cruces de z-score → descuento/precio a +1/+3/+6 meses, tasa de acierto) + panel. La muestra crece con cada día de serie acumulada — se documenta como tal | ✅ Hecho 2026-07-13 (migración 0008 + panel en Grafana); primeros resultados dominados por el cluster de 2026-03 (muestra crece) |
| 9 | Observabilidad: Prometheus (`/metrics` en el poller, Pushgateway para CronJobs) + PVC (el trofeo stateful) + antes/después del panel-hueco de Pipeline Health | 🔄 Commits 1-2 (Prometheus+PVC, `/metrics` del poller) hechos y validados offline 2026-07-13 (decisiones 41-43); falta Pushgateway, paneles y validación in-cluster con la 0.6.0 |
| 10 | Calidad de dato como framework declarativo: checks SQL en config (frescura, huecos, divergencias), panel "Data Quality", degradación automática de `nav_quality`; el cross-check NAV (CEFConnect vs ticker X…X) como primer check | ⬜ |
| 11 | Carga útil final y portada: Helm chart ("portabilidad = el chart", decisión #5 del brief), score compuesto opcional (documentado como discutible), README definitivo con diagrama, sección "la tesis es intercambiable; la plataforma, no" y guía de portado a otros dominios | ⬜ |

Transversal: runbook `deploy/DEPLOY_UBUNTU.md` en cuanto haya acceso al servidor (idealmente tras la fase 7: ArgoCD se instala allí directamente).

## Pendientes / a revisar más adelante

- ~~Push de la 0.4.0 a GHCR~~ → resuelto 2026-07-08 (ver Estado general: el culpable era el pipe de PowerShell, no el token). El despliegue en el Ubuntu ya no tiene bloqueos técnicos, solo falta acceso al servidor.
- ~~**^W5000 no tiene datos 1m en Yahoo**~~ → resuelto en la fase 5¾ (decisión #34: clave `intraday_exclude` en config; ^W5000 sigue en el carril diario para el Buffett pero sale del set intradía).

- **Secretos (#7):** revisar la arquitectura en detalle al llegar a la fase GitOps.
- **Cross-check NAV automatizado (fase de calidad/alertas):** comparar el NAV de CEFConnect con el ticker `X...X` de yfinance y degradar `quality` a `estimado` si divergen más de una tolerancia. Evidencia que lo justifica (2026-07-06): el **último** punto de CEFConnect para FSCO (7,14 el 7/02) difiere un 2,4% de XFSCX (6,97) mientras los días anteriores coinciden al céntimo → el punto más reciente puede ser preliminar. Ojo: el `NAVTicker` de ECAT es `ECAT` a secas (rareza de la API).
- **BAMLH0A0HYM2 limitada a ~3 años vía API** (restricción de licencia ICE, verificada en el payload de bronze: se pidió desde 2015 y FRED devolvió desde 2023-07-04). Suficiente de sobra para el z-score a 1 año; documentar en el README como limitación conocida.
- **Backfill de NAV limitado a 1 año** (la API de CEFConnect no da más histórico diario): los descuentos anteriores a 2025-07 no existirán — hueco documentado, no reparable (previsto en el brief).
- ~~Panel de yield por CEF (pedido 2026-07-07)~~ → resuelto en la mini-fase yield (decisiones #30-33).
- ~~Festivos USA (Fase 4)~~ → resuelta: `exchange_calendars` (decisión #20).
- **Publicar la imagen antes de k3s en el Ubuntu (Fase 3):** el k3d local puede importarla con `k3d image import sentinel:dev`; el k3s del servidor tira de GHCR. Desde la fase 6 la publicación es automática al etiquetar `v*` (ya no `docker push` a mano).
- ~~**Lock de dependencias:**~~ → resuelto en la fase 6 (decisión #36: `uv.lock` con hashes + `uv sync --locked` en un Dockerfile por capas; el build dejó de instalar "lo último" en cada `docker build`).

---

*El principio transversal de todas estas decisiones: elegir lo simple sabiendo cuál es la puerta de escape, externalizar lo irremplazable, y empujar la diferenciación hacia la capa Kubernetes.*
